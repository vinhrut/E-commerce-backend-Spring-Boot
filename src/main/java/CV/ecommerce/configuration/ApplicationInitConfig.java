package CV.ecommerce.configuration;

/**
 * ⚠️ LƯU Ý: Lớp này đã được làm trống.
 * Toàn bộ logic khởi tạo dữ liệu (tạo Admin mặc định, v.v...)
 * đã được chuyển sang DataInitializer.java để dễ dàng quản lý tập trung.
 */
public class ApplicationInitConfig {
    // Left intentionally empty
}
